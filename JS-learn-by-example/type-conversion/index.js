let age = window.prompt("How old are you?");

age = Number(age); // Number is an object kinda fn that takes any and returns f64 
//console.log(typeof age);  prints the data type

// other methods
age = String(32);
age = Boolean(""); //returns false for empty
